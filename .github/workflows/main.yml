name: 'Build Creation'

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Install and setup node version
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'
      
      - name: Install dependencies
        run: npm install
        
      - name: Create project build
        run: npm run build # Trying to create build only

      - name: Creating a build directory
        run: mkdir build
      
      - name: Moving all the files to the build directory
        run: 
          mv ./* build/
          # mv .next/* build/

      # - name: Copy the project created files into the root directory
      #   run: cp -r .next/* ./
      
      # - name: Add the local build to the repository
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: next-build
      #     path: .next/
      #     if-no-files-found: error  # This will make the workflow fail if no files are found